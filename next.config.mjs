/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: 'awj-datathon',
    distDir: 'dist',
    images: {
        unoptimized:true,
    }
};

export default nextConfig;
